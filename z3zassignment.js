import React from 'react';
import {createRoot} from 'react-dom/client';

/**
● - intialize `npm` into your repo - npm init
● - install `react` and `react-dom` - npm i react react-dom
● - remove CDN links of react  - removed CDN links
● - install parcel - npm i parcel(bundler zero config)
● - ignite your app with parcel - npx parcel index.html(npx for executing parcel and index.html is starting paoint remove if conflict with main in package.json)
● - add scripts for “start” and “build” with parcel co"mmands 
{"scripts" : {
    "start" : "parcel index.html",
    "build" : "parcel build index.html",
    "test" : "jest"
}}
● - add `.gitignore` file
add node_modules and parcel-cache
● - add `browserlists`
list all browser where we want our code compatibility
● - build a production version of your code using `parcel build`
npm run build
 */